package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Two Professional Contrast Colors for Elite Reaction Athletic Performance
// Primary Focus Color (Speed, Action, Targets, Latency, Verified)
// Calibrated to a rich, non-blinding athletic performance emerald (eye-friendly, anti-glare)
val SpeedCyan = Color(0xFF00B87A)
val SpeedCyanSoft = Color(0x2800B87A)

// Secondary Contrast Color (Triggers, Inhibitions, Alerts, Attentional Cues)
val SignalAmber = Color(0xFFFF9F0A)
val SignalAmberSoft = Color(0x24FF9F0A)

// Core Dark Architectural Palette
val DarkBackground = Color(0xFF0A0D14)
val CharcoalCard = Color(0xFF111622)
val CharcoalCardElevated = Color(0xFF182030)
val BorderSubtle = Color(0xFF1F293D)
val BorderActive = SpeedCyan

// Aliased semantic tokens mapping strictly into the two-color performance system
val BrandAccent = SpeedCyan
val BrandAccentSoft = SpeedCyanSoft
val VisionTeal = SpeedCyan
val VisionTealSoft = SpeedCyanSoft
val SportGreen = SpeedCyan
val CoolBlue = SpeedCyan

val AmberAlert = SignalAmber
val CoralWarning = Color(0xFFFF453A) // Signal warning / errors

// Typography Palette (Slate scale for maximum high-contrast legibility)
val TextPrimary = Color(0xFFF8FAFC)
val TextMuted = Color(0xFF94A3B8)
val TextSubtle = Color(0xFF64748B)
val TextInverse = Color(0xFF0A0D14)
